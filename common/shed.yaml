wifi:
  ssid: !secret shed_ssid
  password: !secret shed_password
  fast_connect: true
  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "ESPHOME-${device_name}"
    password: "12345678"
